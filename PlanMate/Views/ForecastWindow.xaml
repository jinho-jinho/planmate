<Window x:Class="PlanMate.Views.ForecastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PlanMate.Converters"
        Title="5일 예보" Height="500" Width="400"
        Language="ko-KR">

    <Window.Resources>
        <local:IconToImageConverter x:Key="IconToImageConverter"/>
    </Window.Resources>

    <ScrollViewer>
        <ItemsControl ItemsSource="{Binding Forecasts}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="8"
                            Background="LightYellow"
                            Margin="8"
                            Padding="10"
                            Width="160">
                        <StackPanel>
                            <TextBlock Text="{Binding DateTime, StringFormat={}{0:MM월 dd일 ddd HH시}}"
                                       FontWeight="Bold" FontSize="12" Margin="0,0,0,6"/>
                            <Image Source="{Binding Icon, Converter={StaticResource IconToImageConverter}}"
                                   Width="40" Height="40" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Description}"
                                       FontSize="12" TextAlignment="Center" Margin="0,6,0,6"/>
                            <TextBlock Text="{Binding Temp, StringFormat={}{0:F1}°}"
                                       FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Window>
